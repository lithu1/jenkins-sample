
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application...'
                // Example: run a Docker build or compile code
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Run test scripts here
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the application...'
                // Deploy using Docker or copy to server
            }
        }
    }
}
